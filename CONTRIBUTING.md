# Contributing

Create a focused branch, keep generated LaTeX files out of commits, and submit changes through a pull request. Run `make check` and `make lint` before pushing when the required tools are available. CI must compile the paper without unresolved references or citations.

Use semantic commit messages where practical, for example `paper: clarify evaluation protocol`, `figures: update latency plot`, or `ci: tighten reference checks`.
